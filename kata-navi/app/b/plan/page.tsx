import { BPlaceholder } from "@/components/BPlaceholder";

export default function BPlanPage() {
  return (
    <BPlaceholder
      step="B-4 育成・定着計画"
      lead="受け継ぎを実行プランに。必須は「内容・担当・期日」だけです。"
      next="この画面はステップ6で実装します。"
    />
  );
}
