import { BPlaceholder } from "@/components/BPlaceholder";

export default function BDashboardPage() {
  return (
    <BPlaceholder
      step="B-1 再現性ダッシュボード"
      lead="属人性の高い業務（感覚×高頻度）から手をつけると、再現性の効果が出やすいです。"
      next="この画面はステップ3で実装します（型・store・ナビは構築済み）。"
    />
  );
}
