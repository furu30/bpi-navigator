import { BPlaceholder } from "@/components/BPlaceholder";

export default function BResultPage() {
  return (
    <BPlaceholder
      step="B-5 定着の確認・レポート"
      lead="「できる人 現在/目標」で定着度を確認。振り返りレポートにまとめます。"
      next="この画面はステップ7で実装します。"
    />
  );
}
