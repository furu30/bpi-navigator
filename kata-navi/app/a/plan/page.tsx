import { StepPlaceholder } from "@/components/StepPlaceholder";

export default function PlanPage() {
  return (
    <StepPlaceholder
      step="A-4 改善計画"
      lead="改善候補を実行プランに。必須は「内容・担当・期日」だけ。手段や効果は任意です。"
    />
  );
}
